/**
 * What a control group holds, as a JS enum module: a set of units, or one
 * building the number reopens wherever the view has gone.
 */
export const units = 1 as const;
export type units = typeof units;
export const building = 2 as const;
export type building = typeof building;
